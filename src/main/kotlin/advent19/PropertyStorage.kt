package advent19

/**
 * Class with program properties.
 */
object PropertyStorage {
    /**
     * Returns, how many overlapping beacons should two scanners contain to make a unification of scanners possible.
     *
     * (imagine we receive this from file with properties, not hardcoded)
     */
    fun getOverlappingBeacons(): Int = 12
}
